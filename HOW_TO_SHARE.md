# Chaos Prank: Live Links & Terminal Commands Guide

## 🌐 1. Live Web Link (Share with Anyone Anywhere)

👉 **`https://pmkaulani.github.io/prank/`**

### How it behaves:
- **On Phones & Tablets (iPhone, iPad, Android Phone, Android Tablet)**:
  - Immediately halts the prank.
  - Types out a customized retro terminal roast with comedic device-specific commentary.
  - Leaves zero doubt that this is a computer prank, not a mobile toy.
- **On Computers & Laptops (Windows, Mac, Chromebook, Linux)**:
  - Zero downloads, zero installations needed.
  - Runs the full cinematic virus popup storm with gap-filling memes, error boxes, sound effects, and the `2411` emergency abort.

---

## 💻 2. Real Terminal Commands (Windows CMD & PowerShell)

### A. Run it on YOUR computer right now
Open **Command Prompt (CMD)**, paste this line, and press **Enter**:
```cmd
cd /d "c:\Users\STD USER\Desktop\work\prank" && run_prank.bat
```

---

### B. 1-Line Command for a FRIEND'S Computer (Runs in 1 Second)
If you want to run it on a friend's PC or laptop directly from a terminal window without downloading zip files manually:

**Option 1: In Command Prompt (CMD)**
Press **`Win + R`**, type `cmd`, press Enter, paste this and hit Enter:
```cmd
curl -sL https://github.com/pmkaulani/prank/archive/refs/heads/main.zip -o p.zip && tar -xf p.zip && cd prank-main && run_prank.bat
```

**Option 2: In PowerShell**
Press **`Win + X`** -> **Terminal / PowerShell**, paste this and hit Enter:
```powershell
curl.exe -sL https://github.com/pmkaulani/prank/archive/refs/heads/main.zip -o p.zip; tar.exe -xf p.zip; cd prank-main; .\run_prank.bat
```

*Why this works on computers with nothing installed:*
- `curl.exe` and `tar.exe` are built into every modern Windows 10 & 11 machine by default.
- It pulls the repository zip directly from your GitHub and extracts it instantly (~0.5s).
- If Python is installed, it runs directly in the real terminal!
- If Python is **NOT** installed, `run_prank.bat` automatically launches the zero-dependency visual engine (`index.html`) in their default browser with no delays or errors!

---

## ⏱️ 3. Pacing & Timing Highlights
- **Terminal Typing**: Slowed down to a deliberate, realistic hacker/installer rhythm (`36ms - 55ms` per character with `0.3s - 0.7s` comedic pauses).
- **Meme Download Progress**: Visual progress bars (`[████████] 100%`) for all 18 memes so the user watches every asset load.
- **Terminal Minimization**: As soon as countdown hits `1...`, the console window automatically minimizes to the taskbar.
- **Meme & Error Gap-Filling**: Popups appear one-by-one with breathing room (starts at 1.35s delay, gradually ramping up) filling the screen systematically so people can actually read each meme.
- **Console Restoration**: The console restores itself back to the foreground for the final cleanup sequence.
- **Emergency Abort**: Type **`2411`** (or press `ESC`) at any second to instantly end the visual show.
